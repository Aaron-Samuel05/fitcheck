[project]
name = "fitcheck-api"
version = "1.0.0"
description = "FitCheck FastAPI backend"
requires-python = "~=3.12.0"

[tool.vercel]
# Keep the Python runtime on the stable 3.12 line. Vercel's Python runtime
# has newer versions available, but FitCheck's pinned backend dependencies
# are validated against Python 3.12.
